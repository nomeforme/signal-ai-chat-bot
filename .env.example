# AI Model API Keys
GOOGLE_AI_STUDIO_API="your_google_ai_studio_api_key_here"
ANTHROPIC_API_KEY="your_anthropic_api_key_here"

# AWS Bedrock (Optional - for Claude models via AWS Bedrock)
AWS_ACCESS_KEY_ID="your_aws_access_key_id_here"
AWS_SECRET_ACCESS_KEY="your_aws_secret_access_key_here"
AWS_REGION="us-east-1"

# Image Generation (Optional - for Flux AI)
FAL_KEY="your_fal_ai_key_here"

# Bot Phone Numbers (comma-separated for multiple bots)
# These correspond to the bots array in config.json by index
# Example: BOT_PHONE_NUMBERS="+1234567890,+0987654321"
BOT_PHONE_NUMBERS="+1234567890"

# Signal CLI REST API URLs (Optional - defaults to localhost)
# When running with Docker Compose, these are automatically set to use container networking
# For non-Docker setups, use the default localhost values
WS_BASE_URL="ws://localhost:8080"
HTTP_BASE_URL="http://localhost:8080"

# NOTE: All other configuration (bot names, models, prompts, etc.) is in config.json
# Copy config.example.json to config.json and edit it to configure your bot(s)
